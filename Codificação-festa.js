var  dataEvento = Date ;
console.log(dataEvento);
    if (dataEvento <= Date )
console.log('cadastro permitido por data valida');
    else (dataEvento > Date)
console.log ('Não permitido data excedida');

var idadePermitida = 18;
   if (idadePermitida >= 18)
console.log('Permitido por idade');
   else idadePermitida < 18
console.log('Não permitida');

var quantidadeDeParticipantes = 100;
console.log(quantidadeDeParticipantes);
if (quantidadeDeParticipantes <= 101)
console.log('Cadastro permitido');
else (quantidadeDeParticipantes > 100)
console.log('cadastro não permitido por ter excedido limite de pessoas');